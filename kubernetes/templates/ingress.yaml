apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: frontend-web
  annotations:
     kubernetes.io/ingress.class: "nginx"
     cert-manager.io/cluster-issuer: {{ .Values.certmanager.issuer }}
spec:
  backend:
    serviceName: frontend
    servicePort: 4001
  tls:
  - hosts:
    - {{ .Values.ingress.hosts.frontend }}
    - {{ .Values.ingress.hosts.websocketserver }}
    secretName: {{ .Values.certmanager.secret }}
  rules:
  - host: {{ .Values.ingress.hosts.frontend }}
    http:
      paths:
      - path: /
        backend:
          serviceName: frontend
          servicePort: 4001
  - host: {{ .Values.ingress.hosts.frontend }}
    http:
        paths:
        - path: /ui/grafana
          backend:
            serviceName: frontend
            servicePort: 4002
  - host: {{ .Values.ingress.hosts.frontend }}
    http:
      paths:
      - path: /keycloak
        backend:
          serviceName: keycloak-http
          servicePort: 4080
  - host: {{ .Values.ingress.hosts.websocketserver }}
    http:
      paths:
      - path: /
        backend:
          serviceName: websocket-server
          servicePort: 5000
